import styled from "styled-components"


export const FindProductContainer = styled.div`
    max-width: 960px;
    width: 100%;

    margin: auto;

    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(284px, 1fr));
    gap: 16px;
`

